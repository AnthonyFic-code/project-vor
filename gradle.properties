# Gradle Properties
org.gradle.jvmargs = -Xmx1G
org.gradle.parallel = true

# Mod Properties
version = 0.1.2
maven_group = io.github.project_vor
archives_base_name = project_vor

# Dependencies are managed at gradle/libs.versions.toml
